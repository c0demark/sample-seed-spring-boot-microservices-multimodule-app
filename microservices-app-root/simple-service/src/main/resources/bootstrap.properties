spring.application.name=@{project.build.finalName}
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.service-id=app-webservices-config-server
logging.file=${spring.application.name}